using FluentValidation;

namespace {{Namespace}};

internal sealed class {{UseCase}}Validator : AbstractValidator<{{UseCase}}{{TypeCapital}}>
{
    public {{UseCase}}Validator()
    {
        // rules
    }
}
